function on_message(self, message_id, message, sender)
	if message_id == hash('trigger_response') then
	if message.enter then
		go.set("#sprite", "tint.w", 0.5)
		print('1')
	else
		go.set("#sprite", "tint.w", 1)
	end
elseif message_id == hash('player_coll') then
	go.delete()
end
end